---
import Section from "@components/Section.astro";

import Logo from "@assets/astro.svg";
import Moon from "@icons/moon.svg";
import Sun from "@icons/sun.svg";

interface Props {
  title: string;
  href: string;
}

const NAV_ITEMS: Props[] = [
  { title: "Home", href: "/" },
  { title: "About", href: "#about" },
  { title: "Projects", href: "#projects" },
  { title: "Contact", href: "#contact" },
];

var logo;
---

<header
  class="sticky top-0 z-50 w-full border-b border-slate-200 dark:border-slate-600"
>
  <Section class="flex-row items-center justify-between py-2">
    <Logo />
    <nav class="flex">
      <ul class="flex">
        {
          NAV_ITEMS.map(({ title, href }) => (
            <li>
              <a href={href}>{title}</a>
            </li>
          ))
        }
      </ul>
      <div>
        <button id="theme-toggle" class="cursor-pointer">
          <Sun class="hidden h-6 w-6 dark:block" />
          <Moon class="block h-6 w-6 dark:hidden" />
        </button>
      </div>
    </nav>
  </Section>
</header>

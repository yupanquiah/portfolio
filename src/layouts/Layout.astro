---
import Head from "@components/Head.astro";

const { lang = "es" } = Astro.props;
---

<!doctype html>
<html lang={lang} data-theme="dark">
  <Head name="Humberto Yupanqui Apaza" />
  <body>
    <div
      class="h-auto w-full bg-slate-50 text-slate-950 dark:bg-slate-950 dark:text-slate-50"
    >
      <slot />
    </div>
  </body>
</html>

<script>
  document.addEventListener("astro:page-load", () => {
    const root = document.documentElement;
    const themeToggle = document.getElementById(
      "theme-toggle",
    ) as HTMLButtonElement;

    const applyTheme = () => {
      const isDark =
        localStorage.theme === "dark" ||
        !(
          "theme" in localStorage &&
          window.matchMedia("(prefers-color-scheme: dark)").matches
        );
      root.setAttribute("data-theme", isDark ? "dark" : "light");
      localStorage.theme = isDark ? "dark" : "light";
    };

    const changeTheme = () => {
      const isDarkMode = root.getAttribute("data-theme") === "dark";
      root.setAttribute("data-theme", isDarkMode ? "light" : "dark");
      localStorage.theme = isDarkMode ? "light" : "dark";
    };

    themeToggle.addEventListener("click", changeTheme);

    applyTheme();
  });
</script>

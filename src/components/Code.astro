---
interface Props {
  tag: string;
  class?: string;
  id?: string;
}

const { tag, id } = Astro.props;
---

<code id={id}
  class:list={[
    "font-monaspace-neon absolute inline-flex p-1 text-[10px] font-extralight opacity-50 dark:opacity-40",
    Astro.props.class,
  ]}
>
  {tag}
</code>
